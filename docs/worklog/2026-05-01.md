# 2026-05-01

## 작업 목적

- 로그인 실패 처리 기준을 역할 전체에 맞게 보정
- 다른 로그인 메뉴에 들어간 사용자가 올바른 아이디/비밀번호를 입력한 경우에만 해당 역할 로그인 페이지로 이동
- 아이디는 맞지만 비밀번호가 틀린 경우에는 역할과 무관하게 자격 증명 오류 메시지를 표시

## 수정한 파일

- `src/main/java/com/ssg/wms/admin/controller/AdminController.java`
- `src/main/java/com/ssg/wms/member/controller/MemberController.java`
- `src/main/java/com/ssg/wms/manager/controller/ManagerController.java`

## 수정 전 코드

```java
Staff staff = adminService.loginCheck(loginId, password);
if (staff == null) {
    prepareLoginFailure(loginId, model);
    return "admin/login";
}
```

```java
private void prepareLoginFailure(String loginId, Model model) {
    Staff staff = adminService.findByLoginId(loginId);
    MemberDTO member = memberService.findByLoginId(loginId);
    ...
}
```

## 수정 후 코드

```java
Staff staff = adminService.loginCheck(loginId, password);
if (staff != null) {
    if (staff.getRole() != Role.ADMIN) {
        model.addAttribute("loginId", loginId);
        model.addAttribute("alertMessage", NO_PERMISSION_MESSAGE);
        model.addAttribute("redirectUrl", resolveRedirectUrlByRole(staff.getRole()));
        return "admin/login";
    }

    session.setAttribute("loginStaff", staff);
    session.setAttribute("loginId", loginId);
    session.setAttribute("role", staff.getRole());
    return "redirect:/admin/dashboard";
}

prepareLoginFailure(loginId, password, model);
return "admin/login";
```

```java
private void prepareLoginFailure(String loginId, String password, Model model) {
    MemberDTO member = memberService.loginCheck(loginId, password);
    model.addAttribute("loginId", loginId);

    if (member != null) {
        model.addAttribute("alertMessage", NO_PERMISSION_MESSAGE);
        model.addAttribute("redirectUrl", resolveRedirectUrlByRole(member.getRole()));
        return;
    }

    model.addAttribute("alertMessage", INVALID_CREDENTIALS_MESSAGE);
}
```

## 수정 이유

- 단순히 `loginId` 존재 여부만으로 권한 불일치를 판단하면, 비밀번호를 틀린 경우에도 다른 역할 로그인 페이지로 보내는 문제가 있었음
- 로그인 메뉴를 잘못 선택했더라도 실제로 입력한 아이디/비밀번호 조합이 다른 역할 계정으로 유효할 때만 이동해야 사용자 기대와 맞음
- 따라서 모든 로그인 컨트롤러에서 `다른 역할로도 실제 인증 성공하는지`를 기준으로 권한 불일치와 비밀번호 오류를 분리함

## 추가로 해야 할 작업

- 관리자, 창고관리자, 회원 로그인 페이지에서 다음 시나리오 수동 확인
- 올바른 타 역할 계정으로 로그인 시도
- 올바른 아이디 + 잘못된 비밀번호 입력
- 존재하지 않는 아이디 입력
- `gradle-wrapper.jar` 복구 후 빌드 검증

---

## 작업 목적

- 역할 판별이 `아이디 존재 여부`가 아니라 `아이디 + 비밀번호 인증 성공 여부` 기준으로 동작하도록 보정

## 수정한 파일

- `src/main/java/com/ssg/wms/admin/controller/AdminController.java`
- `src/main/java/com/ssg/wms/member/controller/MemberController.java`
- `src/main/java/com/ssg/wms/manager/controller/ManagerController.java`

## 수정 전 코드

```java
private void prepareLoginFailure(String loginId, Model model) {
    Staff staff = adminService.findByLoginId(loginId);
    MemberDTO member = memberService.findByLoginId(loginId);
    ...
}
```

## 수정 후 코드

```java
private void prepareLoginFailure(String loginId, String password, Model model) {
    MemberDTO member = memberService.loginCheck(loginId, password);
    model.addAttribute("loginId", loginId);

    if (member != null) {
        model.addAttribute("alertMessage", NO_PERMISSION_MESSAGE);
        model.addAttribute("redirectUrl", resolveRedirectUrlByRole(member.getRole()));
        return;
    }

    model.addAttribute("alertMessage", INVALID_CREDENTIALS_MESSAGE);
}
```

```java
private void prepareLoginFailure(String loginId, String password, Model model) {
    Staff staff = adminService.loginCheck(loginId, password);
    MemberDTO member = memberService.loginCheck(loginId, password);
    model.addAttribute("loginId", loginId);

    if (staff != null && staff.getRole() != Role.MANAGER) {
        model.addAttribute("alertMessage", NO_PERMISSION_MESSAGE);
        model.addAttribute("redirectUrl", resolveRedirectUrlByRole(staff.getRole()));
        return;
    }

    if (member != null) {
        model.addAttribute("alertMessage", NO_PERMISSION_MESSAGE);
        model.addAttribute("redirectUrl", resolveRedirectUrlByRole(member.getRole()));
        return;
    }

    model.addAttribute("alertMessage", INVALID_CREDENTIALS_MESSAGE);
}
```

## 수정 이유

- 기존 구현은 동일한 `loginId` 가 존재하면 비밀번호가 틀려도 타 역할 로그인 페이지로 이동할 수 있었음
- 사용자 요구사항은 `정확한 아이디와 비밀번호를 다른 역할 로그인 화면에 입력한 경우만` 올바른 로그인 페이지로 보내는 것이므로, 역할 판별 기준을 실제 인증 성공 여부로 변경함

---

## 작업 목적

- 창고관리자 메뉴에서 재고 화면 진입 시 `Admin Menu` 로 바뀌지 않도록 역할별 헤더/푸터 유지

## 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/stock-list.jsp`
- `src/main/webapp/WEB-INF/views/stock/stock-detail.jsp`
- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`
- `src/main/webapp/WEB-INF/views/stock/product.jsp`

## 수정 전 코드

```jsp
<c:set var="pageActive" value="stock_list" scope="request"/>
<%@ include file="../admin/admin-header.jsp" %>
...
<%@ include file="../admin/admin-footer.jsp" %>
```

## 수정 후 코드

```jsp
<c:set var="pageActive" value="stock_list" scope="request"/>
<c:choose>
    <c:when test="${sessionScope.role == 'MANAGER'}">
        <jsp:include page="/WEB-INF/views/warehousemanager/manager-header.jsp" />
    </c:when>
    <c:otherwise>
        <jsp:include page="/WEB-INF/views/admin/admin-header.jsp" />
    </c:otherwise>
</c:choose>
...
<c:choose>
    <c:when test="${sessionScope.role == 'MANAGER'}">
        <jsp:include page="/WEB-INF/views/warehousemanager/manager-footer.jsp" />
    </c:when>
    <c:otherwise>
        <jsp:include page="/WEB-INF/views/admin/admin-footer.jsp" />
    </c:otherwise>
</c:choose>
```

## 수정 이유

- 재고 관련 JSP들이 `admin-header.jsp` 와 `admin-footer.jsp` 를 고정 include 하고 있어, 창고관리자가 접근해도 레이아웃이 관리자 메뉴로 전환되고 있었음
- 세션의 `role` 값을 기준으로 관리자/창고관리자 헤더와 푸터를 분기하면 같은 재고 화면을 재사용하면서도 현재 역할 메뉴를 유지할 수 있음

---

## 작업 목적

- 로고 클릭 시 `index.html` 로 이동하면서 발생하던 404를 제거하고 첫 페이지 `/login` 으로 이동하도록 수정

## 수정한 파일

- `src/main/webapp/WEB-INF/views/admin/login.jsp`
- `src/main/webapp/WEB-INF/views/warehousemanager/login.jsp`
- `src/main/webapp/WEB-INF/views/member/login.jsp`
- `src/main/webapp/WEB-INF/views/warehousemanager/manager-header.jsp`
- `src/main/webapp/WEB-INF/views/member/member-header.jsp`
- `src/main/webapp/WEB-INF/views/admin/register.jsp`
- `src/main/webapp/WEB-INF/views/member/register.jsp`
- `src/main/webapp/WEB-INF/views/member/success.jsp`

## 수정 전 코드

```jsp
<a href="index.html" class="app-brand-link gap-2">
```

## 수정 후 코드

```jsp
<a href="${pageContext.request.contextPath}/login" class="app-brand-link gap-2">
```

```jsp
<a href="${pageContext.request.contextPath}/login" class="app-brand-link">
```

## 수정 이유

- 여러 로그인/헤더 화면의 로고 링크가 실제 존재하지 않는 `index.html` 을 가리키고 있어 클릭 시 404가 발생했음
- 공통 첫 페이지는 `CommonController` 의 `/login` 이므로, 브랜드 로고는 해당 경로로 보내는 것이 현재 프로젝트 구조와 일치함

---

## 작업 목적

- 창고관리자 로그아웃 404 수정
- 창고관리자 메뉴에서 현재 위치한 재고 하위 메뉴 활성화 표시 복구

## 수정한 파일

- `src/main/webapp/WEB-INF/views/warehousemanager/manager-header.jsp`
- `src/main/webapp/WEB-INF/views/warehousemanager/connect.jsp`

## 수정 전 코드

```jsp
<a href="${pageContext.request.contextPath}/manager/mypage">
<a href="${pageContext.request.contextPath}/manager/logout">
```

```jsp
<li class="menu-item">
    <a href="/warehousemanager" class="menu-link">
```

```jsp
<li class="menu-item">
    <a href="javascript:void(0);" class="menu-link menu-toggle">
```

## 수정 후 코드

```jsp
<a href="${pageContext.request.contextPath}/warehousemanager/mypage">
<a href="${pageContext.request.contextPath}/warehousemanager/logout">
```

```jsp
<li class="menu-item ${pageActive == 'dashboard' ? 'active' : ''}">
    <a href="${pageContext.request.contextPath}/warehousemanager" class="menu-link">
```

```jsp
<li class="menu-item ${(pageActive == 'stock_list' || pageActive == 'stock_detail' || pageActive == 'physical_inventory' || pageActive == 'product_list') ? 'active open' : ''}">
```

```jsp
<li class="menu-item ${(pageActive == 'stock_list' || pageActive == 'stock_detail') ? 'active' : ''}">
<li class="menu-item ${pageActive == 'physical_inventory' ? 'active' : ''}">
<li class="menu-item ${pageActive == 'product_list' ? 'active' : ''}">
```

```jsp
<c:set var="pageActive" value="dashboard" scope="request"/>
```

## 수정 이유

- 드롭다운의 마이페이지/로그아웃 링크가 실제 컨트롤러 경로인 `/warehousemanager/...` 가 아니라 `/manager/...` 를 가리켜 404가 발생했음
- 창고관리자 헤더에는 `pageActive` 를 사용하는 활성화 조건이 없어, 재고 조회/재고 실사/품목 현황 진입 시 현재 메뉴 표시가 동작하지 않았음
- 메인 대시보드 화면에도 `pageActive` 값을 내려주도록 보완해 대시보드 메뉴 활성화도 함께 맞춤
---

## 작업 목적

- 창고관리자 메뉴의 `창고 관리` 하위 화면 전체가 404 나는 문제 수정

## 수정한 파일

- `src/main/webapp/WEB-INF/views/warehousemanager/manager-header.jsp`
- `src/main/webapp/WEB-INF/views/admin/admin-header.jsp`
- `src/main/webapp/WEB-INF/views/warehouse/list.jsp`
- `src/main/webapp/WEB-INF/views/warehouse/register.jsp`
- `src/main/webapp/WEB-INF/views/warehouse/detail.jsp`
- `src/main/webapp/WEB-INF/views/warehouse/modify.jsp`

## 수정 전 코드

```jsp
<a href="html/pages-account-settings-account.html" class="menu-link">
```

```jsp
<%@ include file="../admin/admin-header.jsp" %>
...
<form action="${pageContext.request.contextPath}/admin/warehouses/register" method="post">
```

## 수정 후 코드

```jsp
<a href="${pageContext.request.contextPath}/mgr/warehouses/register" class="menu-link">
<a href="${pageContext.request.contextPath}/mgr/warehouses" class="menu-link">
<a href="${pageContext.request.contextPath}/mgr/warehouses/location" class="menu-link">
```

```jsp
<c:set var="isManagerView" value="${sessionScope.role == 'MANAGER'}" />
<c:set var="warehouseBasePath" value="${isManagerView ? '/mgr/warehouses' : '/admin/warehouses'}" />
<c:choose>
    <c:when test="${isManagerView}">
        <jsp:include page="/WEB-INF/views/warehousemanager/manager-header.jsp" />
    </c:when>
    <c:otherwise>
        <jsp:include page="/WEB-INF/views/admin/admin-header.jsp" />
    </c:otherwise>
</c:choose>
```

## 수정 이유

- 창고관리자 메뉴 링크가 실제 컨트롤러 경로가 아닌 placeholder HTML 경로를 가리켜 404가 발생했다.
- 공용 `warehouse/*.jsp` 가 관리자 헤더/푸터와 관리자 경로를 고정 사용하고 있어, 창고관리자가 진입하면 같은 화면 안에서도 경로와 메뉴가 맞지 않았다.
- 세션 `role` 기준으로 헤더/푸터와 `warehouseBasePath` 를 분기하면 같은 JSP를 유지하면서도 관리자와 창고관리자 경로를 올바르게 사용할 수 있다.
