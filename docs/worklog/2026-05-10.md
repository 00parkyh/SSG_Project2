# 작업 목적

- 재고 실사 화면과 관련 백엔드 코드에서 깨진 한글을 복구한다.
- 재고 실사 화면에서 현재 로그인한 담당자 이름과 ID가 정상적으로 채워지도록 수정한다.

# 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`
- `src/main/java/com/ssg/wms/product_stock/controller/PhysicalInventoryController.java`
- `src/main/java/com/ssg/wms/product_stock/service/PhysicalInventoryServiceImpl.java`
- `src/main/java/com/ssg/wms/common/AdjustmentStatus.java`

# 수정 전 코드

```jsp
<input type="text"
       id="managerNameDisplay"
       class="form-control"
       value="${not empty loginAdmin ? loginAdmin.staffName : '담당자 정보 없음'}"
       readonly>

<input type="hidden"
       id="managerId"
       name="managerId"
       value="${not empty loginAdmin ? loginAdmin.staffId : ''}"
       required>
```

```javascript
const managerIdElement = document.getElementById('managerId');

const formData = {
    piDate: piDateElement.value,
    piState: piStateElement.value,
    managerId: managerIdElement.value,
    warehouseId: warehouseElement.value,
    sectionId: sectionElement.value
};
```

```java
public enum AdjustmentStatus {
    NOT_ADJUSTED("議곗젙 誘명븘??),
    WAITING("議곗젙 ?湲?),
    IN_PROGRESS("議곗젙 吏꾪뻾以?),
    COMPLETED("議곗젙 ?꾨즺");
```

# 수정 후 코드

```jsp
<input type="text"
       id="staffNameDisplay"
       class="form-control"
       value="${not empty currentStaffName ? currentStaffName : '담당자 정보 없음'}"
       readonly>

<input type="hidden"
       id="staffId"
       name="staffId"
       value="${not empty currentStaffId ? currentStaffId : ''}"
       required>
```

```javascript
const staffIdElement = document.getElementById('staffId');

const formData = {
    piDate: piDateElement.value,
    piState: piStateElement.value,
    staffId: staffIdElement.value,
    warehouseId: warehouseElement.value,
    sectionId: sectionElement.value
};
```

```java
private void populateCurrentStaff(Model model, HttpSession session) {
    StaffDTO loginManager = (StaffDTO) session.getAttribute("loginManager");
    StaffDTO loginAdmin = (StaffDTO) session.getAttribute("loginAdmin");
    Staff loginStaff = (Staff) session.getAttribute("loginStaff");

    if (loginManager != null) {
        model.addAttribute("currentStaffId", loginManager.getStaffId());
        model.addAttribute("currentStaffName", loginManager.getStaffName());
        return;
    }

    if (loginAdmin != null) {
        model.addAttribute("currentStaffId", loginAdmin.getStaffId());
        model.addAttribute("currentStaffName", loginAdmin.getStaffName());
        return;
    }

    if (loginStaff != null) {
        model.addAttribute("currentStaffId", loginStaff.getStaffId());
        model.addAttribute("currentStaffName", loginStaff.getStaffName());
    }
}
```

```java
public enum AdjustmentStatus {
    NOT_ADJUSTED("조정 미완료"),
    WAITING("조정 대기"),
    IN_PROGRESS("조정 진행중"),
    COMPLETED("조정 완료");
```

# 수정 이유

- 재고 실사 화면에서 `loginAdmin` 에만 의존하고 있어 창고관리자 세션에서는 담당자 정보가 비어 있었다.
- 프론트 전송 필드명이 `managerId` 였지만 요청 DTO는 `staffId` 를 사용하고 있어 이름과 ID 체계가 맞지 않았다.
- 실사 화면과 조정 상태 enum, 등록/수정 메시지에 깨진 한글이 남아 있어 사용자 화면과 알림 문구가 손상되고 있었다.

# 추가로 해야 할 작업

- 브라우저에서 관리자와 창고관리자 각각 로그인한 뒤 재고 실사 화면의 담당자 이름 표시를 확인한다.
- 실사 등록과 실사 수정 모달의 안내 문구가 정상 한글로 노출되는지 확인한다.

## 추가 작업

### 작업 목적

- 재고 실사 기능 관련 파일에 남아 있던 깨진 한글 문자열을 모두 복구한다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`
- `src/main/java/com/ssg/wms/product_stock/controller/PhysicalInventoryController.java`
- `src/main/java/com/ssg/wms/product_stock/service/PhysicalInventoryServiceImpl.java`
- `src/main/java/com/ssg/wms/product_stock/dto/PhysicalInventoryRequest.java`
- `src/main/java/com/ssg/wms/product_stock/dto/PhysicalInventoryDTO.java`

### 수정 전 코드

```java
return ResponseEntity.status(HttpStatus.CREATED).body(Map.of(
        "message", "?깅줉 ?깃났",
        "count", registeredCount
));
```

```java
throw new IllegalArgumentException("?좏깮??李쎄퀬/?뱀뀡???꾩옱 ?ш퀬媛 議댁옱?섏? ?딆뒿?덈떎. ?ㅼ궗 ?깅줉 遺덇?.");
```

```jsp
<h5 class="mb-0">?ㅼ궗 ?깅줉</h5>
<option value="吏꾪뻾以?>吏꾪뻾以?/option>
```

### 수정 후 코드

```java
return ResponseEntity.status(HttpStatus.CREATED).body(Map.of(
        "message", "등록 성공",
        "count", registeredCount
));
```

```java
throw new IllegalArgumentException("선택한 창고/섹션에 현재 재고가 존재하지 않습니다. 실사 등록 불가.");
```

```jsp
<h5 class="mb-0">실사 등록</h5>
<option value="진행중">진행중</option>
```

### 수정 이유

- 재고 실사 화면과 API 응답 메시지, 서비스 예외 메시지에 깨진 한글이 남아 있어 사용자 화면과 알림 문구를 정상적으로 읽을 수 없었다.
- JSP 일부 태그가 문자열 깨짐으로 인해 마크업까지 손상되어 있어 화면 동작 안정성에도 문제가 있었다.

### 추가로 해야 할 작업

- 브라우저에서 재고 실사 화면의 제목, 버튼, 드롭다운, 모달 문구가 정상 한글로 보이는지 확인한다.
- 등록/수정 실패 케이스에서 alert 메시지가 정상 한글로 출력되는지 확인한다.

## 추가 작업 2

### 작업 목적

- 기존 실사 리스트 영역은 보존한 채, 새 JSP 파일로 분리하여 이후 리스트 개편 작업을 위한 기반을 만든다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`
- `src/main/webapp/WEB-INF/views/stock/physical-inventory-list-v2.jsp`

### 수정 전 코드

```jsp
<div class="card2">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">실사 리스트</h5>
    </div>
    ...
</div>
```

### 수정 후 코드

```jsp
<%--
<div class="card2">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">실사 리스트</h5>
    </div>
    ...
</div>
--%>

<jsp:include page="/WEB-INF/views/stock/physical-inventory-list-v2.jsp" />
```

```jsp
<div class="card2">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">실사 리스트</h5>
    </div>
    ...
</div>
```

### 수정 이유

- 기존 실사 리스트 코드는 그대로 남겨두고, 새 JSP에서 동일한 리스트 마크업을 먼저 띄워 이후 UI 개편을 안전하게 진행하기 위함이다.

### 추가로 해야 할 작업

- 브라우저에서 기존과 동일하게 실사 리스트 테이블과 페이지네이션이 출력되는지 확인한다.
- 이후 `physical-inventory-list-v2.jsp` 에서만 리스트 UI를 점진적으로 개편한다.

## 추가 작업 3

### 작업 목적

- 분리한 실사 리스트 JSP에서 한글이 깨지는 문제를 방지하기 위해 페이지 인코딩을 명시한다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory-list-v2.jsp`

### 수정 전 코드

```jsp
<div class="card2">
```

### 수정 후 코드

```jsp
<%@ page language="java" contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %>

<div class="card2">
```

### 수정 이유

- `include` 되는 별도 JSP 파일에서도 UTF-8 인코딩을 명시해 한글 깨짐 가능성을 줄이기 위함이다.

## 추가 작업 4

### 작업 목적

- 실사 리스트가 DB에서 데이터를 가져와 화면에 표시하는 연결을 프론트에서 임시로 중지한다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`

### 수정 전 코드

```javascript
function searchPhysicalInventoryList(page) {
    document.getElementById('pi-tbody').innerHTML = '<tr><td colspan="10" class="text-center">실사 목록 검색 중...</td></tr>';
    const queryString = getPhysicalInventorySearchParams(page);
    const apiUrl = '/physical-inventory/search?' + queryString;
```

### 수정 후 코드

```javascript
const isPhysicalInventoryListFetchEnabled = false;

function searchPhysicalInventoryList(page) {
    if (!isPhysicalInventoryListFetchEnabled) {
        physicalInventoryDataList = [];
        document.getElementById('pi-tbody').innerHTML = '';
        document.getElementById('pi-pagination-ul').innerHTML = '';
        return;
    }

    document.getElementById('pi-tbody').innerHTML = '<tr><td colspan="10" class="text-center">실사 목록 검색 중...</td></tr>';
    const queryString = getPhysicalInventorySearchParams(page);
    const apiUrl = '/physical-inventory/search?' + queryString;
```

### 수정 이유

- 백엔드 API와 DB는 유지한 채, 화면에서만 실사 리스트 조회를 잠시 끊어 새 리스트 개편 작업을 안전하게 진행하기 위함이다.

## 추가 작업 5

### 작업 목적

- 실사 리스트 조회 중지 상태는 유지하되, 화면에 표시되던 임시 안내 문구를 제거한다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`

### 수정 전 코드

```javascript
document.getElementById('pi-tbody').innerHTML = '<tr><td colspan="10" class="text-center">실사 리스트 조회 연결이 일시 중지되었습니다.</td></tr>';
```

### 수정 후 코드

```javascript
document.getElementById('pi-tbody').innerHTML = '';
```

### 수정 이유

- 리스트 개편 작업 중 불필요한 임시 문구를 화면에 노출하지 않기 위함이다.

## 추가 작업 6

### 작업 목적

- `physical-inventory.jsp` 에 기존 실사 리스트 주석 블록을 유지하고, 분리된 `v2` 파일은 실사 리스트만 담당하도록 상태를 맞춘다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`

### 수정 전 코드

```jsp
<jsp:include page="/WEB-INF/views/stock/physical-inventory-list-v2.jsp" />
```

### 수정 후 코드

```jsp
<%--
<div class="card2">
    ...
</div>
--%>

<jsp:include page="/WEB-INF/views/stock/physical-inventory-list-v2.jsp" />
```

### 수정 이유

- 원본 `physical-inventory.jsp` 의 기존 리스트 코드는 보존하고, 새 `physical-inventory-list-v2.jsp` 에서는 실사 리스트만 유지하는 요청사항에 맞추기 위함이다.

## 추가 작업 7

### 작업 목적

- `physical-inventory-V2.jsp` 는 실사 리스트 영역만 담당하도록 정리하고, 임시로 만들었던 `physical-inventory-list-v2.jsp` 파일은 제거한다.

### 수정한 파일

- `src/main/webapp/WEB-INF/views/stock/physical-inventory.jsp`
- `src/main/webapp/WEB-INF/views/stock/physical-inventory-V2.jsp`
- `src/main/webapp/WEB-INF/views/stock/physical-inventory-list-v2.jsp`

### 수정 전 코드

```jsp
<jsp:include page="/WEB-INF/views/stock/physical-inventory-list-v2.jsp" />
```

```jsp
<%@ page language="java" contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<c:set var="pageActive" value="physical_inventory" scope="request"/>
...
```

### 수정 후 코드

```jsp
<jsp:include page="/WEB-INF/views/stock/physical-inventory-V2.jsp" />
```

```jsp
<%@ page language="java" contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %>

<div class="card2">
    ...
</div>
```

### 수정 이유

- 실제로 사용할 `V2` 파일은 실사 리스트 영역만 포함해야 하고, 중간에 만든 `physical-inventory-list-v2.jsp` 는 더 이상 필요하지 않기 때문이다.
